{
  "name": "mock-sus-api",
  "version": "1.0.0",
  "description": "Mock Server para simular APIs externas do sistema SUS",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "start:routes": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
